# Changelog

## [Unreleased]

- Added offline CLI and output-safety tests.
- Added `--help` and `--overwrite` options.
- Added explicit `both` fallback reporting.
- Added cached virtual environments for CPU, MLX, and OpenCC dependencies.
- Added temporary output publishing and preservation of failed runs.
- Added GitHub Actions CI and repository contribution/security templates.
