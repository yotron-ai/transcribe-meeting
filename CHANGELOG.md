# 變更記錄

## [未發布]

- CPU 預設依賴更新為 `openai-whisper==20250625`，修復舊版在乾淨環境建立套件時缺少 `pkg_resources` 的安裝錯誤；既有快取環境不會自動升級。
- 新增使用合成英文語音的真實 CPU 轉錄工作流程，分別驗證首次與快取後的五種輸出、OpenCC 及品質稽核，並保存可重現的測試報告。

- 有 `ffprobe` 時，在準備引擎前拒絕沒有音訊軌的影片或字幕檔；含音訊軌的影片仍可轉錄。
- 新增無音軌攔截及含音軌影片轉錄的回歸測試。
- 補充 CLI 下載步驟、平台驗證範圍、首次試用指南與使用回饋範本。

- 新增 `--help`、`--overwrite` 與輸入媒體驗證。
- 明確處理 MLX、CPU 與 `both` 模式的 fallback 行為。
- 新增 CPU、MLX、OpenCC 專用快取虛擬環境與固定預設版本。
- 新增暫存輸出、完整性檢查、品質稽核與既有輸出備份。
- 新增 12 個不需下載模型的離線測試。
- 新增 GitHub Actions、Dependabot、Issue 範本、PR 範本與貢獻／安全性文件。
- 修正 macOS 下以 `-` 開頭的輸入路徑解析，以及非數字音檔長度的 fallback。
